# Timeless API token (64-char hex), from the Timeless dashboard.
# Sent as: Authorization: Bearer <token>
TIMELESS_API_TOKEN=

# GitHub token for committing notes into OmerKeinan1 repos.
# Leave blank for now; the destination side is not wired up yet.
GITHUB_TOKEN=

# Anthropic key for the LLM routing fallback (unmatched meetings).
# Leave blank for now; rules-only routing does not need it.
ANTHROPIC_API_KEY=
